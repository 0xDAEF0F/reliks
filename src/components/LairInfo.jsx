function LairInfo() {
  // Show the current count of the lair, users data, current lair entry price.
  // Show some discord server data and button with discord server invitation.
  return <div>LairInfo</div>
}

export default LairInfo
